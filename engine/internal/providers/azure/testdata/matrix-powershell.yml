jobs:
  - job: test
    strategy:
      maxParallel: 2
      matrix:
        linux:
          NODE_VERSION: "24"
        current:
          NODE_VERSION: "26"
    steps:
      - pwsh: Write-Output "Node $env:NODE_VERSION"
      - task: Cache@2
        inputs:
          key: npm
          path: .npm
